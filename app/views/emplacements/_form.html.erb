<%= form_for(@emplacement) do |f| %>
  <% if @emplacement.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@emplacement.errors.count, "error") %> prohibited this emplacement from being saved:</h2>

      <ul>
      <% @emplacement.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :salle_id %><br />
    <%= f.number_field :salle_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
