<%= form_for(@salle) do |f| %>
  <% if @salle.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@salle.errors.count, "error") %> prohibited this salle from being saved:</h2>

      <ul>
      <% @salle.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nom %><br />
    <%= f.text_field :nom %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
