<h1>Listing salles</h1>

<table>
  <tr>
    <th>Nom</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @salles.each do |salle| %>
  <tr>
    <td><%= salle.nom %></td>
    <td><%= link_to 'Show', salle %></td>
    <td><%= link_to 'Edit', edit_salle_path(salle) %></td>
    <td><%= link_to 'Destroy', salle, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Salle', new_salle_path %>
